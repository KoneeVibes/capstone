import { SearchWrapper } from "./styled";

export const Search = () => {
	return (
		<SearchWrapper
			maxWidth={false}
			sx={{
				padding: "0 !important",
			}}
        >
            
        </SearchWrapper>
	);
};
