import { Stack, styled } from "@mui/material";

export const SearchFormWrapper = styled(Stack)(({ theme }) => {
	return {
		[theme.breakpoints.up("miniTablet")]: {
			
		},
		[theme.breakpoints.up("tablet")]: {
			
		},
	};
});
